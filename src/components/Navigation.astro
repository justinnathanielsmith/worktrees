<nav>
    <div class="container">
        <a href="#" class="logo impact">BARE<span>HUB</span></a>
        <div class="nav-links">
            <a href="#protocol">PROTOCOL</a>
            <a href="#maintenance">COMMANDS</a>
            <div class="nav-status">STATUS: OPTIMIZED</div>
        </div>
    </div>
</nav>

<style>
    nav {
        position: fixed;
        top: 0;
        width: 100%;
        z-index: 1000;
        background: rgba(10, 10, 10, 0.9);
        backdrop-filter: blur(20px);
        border-bottom: var(--border-width) solid var(--gray);
        padding: 1.25rem 0;
    }

    nav .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .logo {
        font-size: 2rem;
        color: var(--white);
        text-decoration: none;
        transition: var(--transition);
        font-weight: 900;
    }

    .logo span {
        color: var(--gold);
    }

    .nav-links {
        display: flex; 
        gap: 3rem; 
        align-items: center;
    }

    .nav-links a {
        color: var(--white); 
        text-decoration: none; 
        font-size: 0.75rem; 
        font-weight: 900; 
        letter-spacing: 0.1em;
    }

    .nav-status {
        font-size: 0.7rem;
        font-weight: 900;
        color: var(--gold);
        border: 1px solid var(--gold);
        padding: 0.2rem 0.6rem;
        text-transform: uppercase;
    }

    @media (max-width: 600px) {
        .nav-links { gap: 1rem; }
        .nav-status { display: none; }
    }
</style>
