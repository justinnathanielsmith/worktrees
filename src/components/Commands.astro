---
---
<section id="maintenance" class="py-24 bg-slate-950">
    <div class="container mx-auto px-4">
        <div class="text-center mb-16 fade-in">
            <h2 class="text-4xl md:text-6xl font-bold text-white mb-4">Workspace Hygiene</h2>
            <p class="text-blue-500 text-lg uppercase tracking-widest font-bold">Maintenance & Cleanup</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 xl:grid-cols-5 gap-8">
            <!-- Tip 1 -->
            <div class="fade-in command-row bg-slate-900 border border-slate-800 p-8 rounded-xl hover:border-blue-500 transition-all group cursor-pointer relative overflow-hidden" data-cmd="git worktree remove <name>">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-blue-900/30 flex items-center justify-center text-blue-400 text-2xl">🧹</div>
                    <h4 class="text-xl font-bold text-white group-hover:text-blue-400 transition-colors">Remove Worktree</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Don't just delete the folder. Unregister it from the engine properly.</p>
                <div class="bg-black/40 p-4 rounded-lg font-mono text-xs text-blue-400 border border-slate-700/50 group-hover:border-blue-500/30 transition-colors">
                    git worktree remove &lt;name&gt;
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-blue-500 text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>

            <!-- Tip 2 -->
            <div class="fade-in command-row bg-slate-900 border border-slate-800 p-8 rounded-xl hover:border-blue-500 transition-all group cursor-pointer relative overflow-hidden" data-cmd="git fetch --prune">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-blue-900/30 flex items-center justify-center text-blue-400 text-2xl">✂️</div>
                    <h4 class="text-xl font-bold text-white group-hover:text-blue-400 transition-colors">Prune Stale</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Remove remote tracking branches that no longer exist on the server.</p>
                <div class="bg-black/40 p-4 rounded-lg font-mono text-xs text-blue-400 border border-slate-700/50 group-hover:border-blue-500/30 transition-colors">
                    git fetch --prune
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-blue-500 text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>

            <!-- Tip 3 -->
            <div class="fade-in command-row bg-slate-900 border border-slate-800 p-8 rounded-xl hover:border-blue-500 transition-all group cursor-pointer relative overflow-hidden" data-cmd="git fetch --all">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-blue-900/30 flex items-center justify-center text-blue-400 text-2xl">🌍</div>
                    <h4 class="text-xl font-bold text-white group-hover:text-blue-400 transition-colors">Fetch All</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Update all remote pointers in the engine from any directory context.</p>
                <div class="bg-black/40 p-4 rounded-lg font-mono text-xs text-blue-400 border border-slate-700/50 group-hover:border-blue-500/30 transition-colors">
                    git fetch --all
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-blue-500 text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>

            <!-- Tip 4 -->
            <div class="fade-in command-row bg-slate-900 border border-slate-800 p-8 rounded-xl hover:border-blue-500 transition-all group cursor-pointer relative overflow-hidden" data-cmd="wt run temp-check 'npm test'">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-blue-900/30 flex items-center justify-center text-blue-400 text-2xl">⚡</div>
                    <h4 class="text-xl font-bold text-white group-hover:text-blue-400 transition-colors">Quick Task</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Execute commands in a temporary worktree that is removed after completion.</p>
                <div class="bg-black/40 p-4 rounded-lg font-mono text-xs text-blue-400 border border-slate-700/50 group-hover:border-blue-500/30 transition-colors">
                    wt run &lt;name&gt; &lt;command&gt;
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-blue-500 text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>

            <!-- Tip 5 -->
            <div class="fade-in command-row bg-slate-900 border border-slate-800 p-8 rounded-xl hover:border-blue-500 transition-all group cursor-pointer relative overflow-hidden" data-cmd="wt sync">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-blue-900/30 flex items-center justify-center text-blue-400 text-2xl">🔄</div>
                    <h4 class="text-xl font-bold text-white group-hover:text-blue-400 transition-colors">Sync Config</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Synchronize configuration files defined in .worktrees.sync across all environments.</p>
                <div class="bg-black/40 p-4 rounded-lg font-mono text-xs text-blue-400 border border-slate-700/50 group-hover:border-blue-500/30 transition-colors">
                    wt sync [name]
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-blue-500 text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>
        </div>
    </div>
</section>

<style>
    .command-row.copied .copy-feedback {
        opacity: 1;
        transform: translateY(0);
    }
</style>

<script>
    document.querySelectorAll('.command-row').forEach(row => {
        row.addEventListener('click', async () => {
            const cmd = row.getAttribute('data-cmd');
            if (!cmd) return;

            try {
                await navigator.clipboard.writeText(cmd);
                row.classList.add('copied');
                setTimeout(() => {
                    row.classList.remove('copied');
                }, 2000);
            } catch (err) {
                console.error('Failed to copy:', err);
            }
        });
    });
</script>
