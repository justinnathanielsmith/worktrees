---
---
<section id="maintenance" class="py-24 bg-cyber-bg">
    <div class="container mx-auto px-4">
        <div class="text-center mb-16 reveal">
            <h2 class="text-4xl md:text-6xl font-black text-white mb-4 impact">GYM HYGIENE</h2>
            <p class="text-slate-400 text-lg uppercase tracking-[0.2em] font-bold">Maintenance & Cleanup</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <!-- Tip 1 -->
            <div class="reveal command-row bg-slate-800/40 p-8 rounded-2xl border border-slate-700 hover:border-cyber-pink transition-all group backdrop-blur-sm cursor-pointer relative overflow-hidden" data-cmd="git worktree remove <name>">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-cyber-pink/20 flex items-center justify-center text-cyber-pink text-2xl">🧹</div>
                    <h4 class="text-2xl font-black text-white impact tracking-wider">Remove Muscle</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Don't just delete the folder. Unregister it from the engine properly.</p>
                <div class="bg-black/60 p-4 rounded-lg font-mono text-xs text-cyber-pink border border-slate-700/50 group-hover:border-cyber-pink/30 transition-colors">
                    git worktree remove &lt;name&gt;
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-cyber-pink text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>

            <!-- Tip 2 -->
            <div class="reveal command-row bg-slate-800/40 p-8 rounded-2xl border border-slate-700 hover:border-cyber-cyan transition-all group backdrop-blur-sm cursor-pointer relative overflow-hidden" data-cmd="git fetch --prune">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-cyber-cyan/20 flex items-center justify-center text-cyber-cyan text-2xl">👻</div>
                    <h4 class="text-2xl font-black text-white impact tracking-wider">Prune Ghosts</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Remove remote tracking branches that no longer exist on the server.</p>
                <div class="bg-black/60 p-4 rounded-lg font-mono text-xs text-cyber-cyan border border-slate-700/50 group-hover:border-cyber-cyan/30 transition-colors">
                    git fetch --prune
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-cyber-cyan text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>

            <!-- Tip 3 -->
            <div class="reveal command-row bg-slate-800/40 p-8 rounded-2xl border border-slate-700 hover:border-cyber-green transition-all group backdrop-blur-sm cursor-pointer relative overflow-hidden" data-cmd="git fetch --all">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-10 h-10 rounded-lg bg-cyber-green/20 flex items-center justify-center text-cyber-green text-2xl">🌍</div>
                    <h4 class="text-2xl font-black text-white impact tracking-wider">Global Fetch</h4>
                </div>
                <p class="text-slate-400 text-sm mb-6 leading-relaxed">Update all remote pointers in the engine from any directory context.</p>
                <div class="bg-black/60 p-4 rounded-lg font-mono text-xs text-cyber-green border border-slate-700/50 group-hover:border-cyber-green/30 transition-colors">
                    git fetch --all
                </div>
                <div class="copy-feedback absolute top-0 right-0 bg-cyber-green text-white text-[0.6rem] font-bold px-2 py-1 opacity-0 transform -translate-y-full transition-all">COPIED</div>
            </div>
        </div>
    </div>
</section>

<style>
    .command-row.copied .copy-feedback {
        opacity: 1;
        transform: translateY(0);
    }
</style>

<script>
    document.querySelectorAll('.command-row').forEach(row => {
        row.addEventListener('click', async () => {
            const cmd = row.getAttribute('data-cmd');
            if (!cmd) return;

            try {
                await navigator.clipboard.writeText(cmd);
                row.classList.add('copied');
                setTimeout(() => {
                    row.classList.remove('copied');
                }, 2000);
            } catch (err) {
                console.error('Failed to copy:', err);
            }
        });
    });
</script>
