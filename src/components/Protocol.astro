---
import CodeBlock from './CodeBlock.astro';
---

<section id="protocol" class="py-24 bg-slate-900 border-t border-slate-800">
    <div class="container mx-auto px-4">
        <div class="flex flex-col md:flex-row items-start gap-16">
            <div class="md:w-1/3">
                <div class="sticky top-32 fade-in space-y-6">
                    <h3 class="text-3xl md:text-4xl font-bold text-white tracking-tight">Setup Guide</h3>
                    <p class="text-slate-400 text-lg leading-relaxed">
                        Follow these steps to initialize your repository with the Bare Hub architecture.
                    </p>
                </div>
            </div>
            
            <div class="md:w-2/3 space-y-12 relative">
                <!-- Step 1 -->
                <div class="fade-in relative bg-slate-800 p-8 rounded-xl border border-slate-700 shadow-sm">
                    <div class="absolute -left-4 top-8 w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center font-bold text-white text-sm hidden md:flex">1</div>
                    <h4 class="text-xl font-bold text-white mb-3">Create The Engine</h4>
                    <p class="text-slate-400 mb-6">Initialize the repository as a bare repo in a hidden directory to decouple history from your workspace.</p>
                    <CodeBlock code="mkdir project && cd project\ngit clone --bare git@github.com:user/repo.git .bare" label="Terminal" />
                </div>

                <!-- Step 2 -->
                <div class="fade-in relative bg-slate-800 p-8 rounded-xl border border-slate-700 shadow-sm">
                    <div class="absolute -left-4 top-8 w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center font-bold text-white text-sm hidden md:flex">2</div>
                    <h4 class="text-xl font-bold text-white mb-3">Connect The Hub</h4>
                    <p class="text-slate-400 mb-6">Point the root Git context to the hidden engine using a gitdir pointer file.</p>
                    <CodeBlock code='echo "gitdir: ./.bare" > .git' label="Terminal" />
                </div>

                <!-- Step 3 -->
                <div class="fade-in relative bg-slate-800 p-8 rounded-xl border border-slate-700 shadow-sm">
                    <div class="absolute -left-4 top-8 w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center font-bold text-white text-sm hidden md:flex">3</div>
                    <h4 class="text-xl font-bold text-white mb-3">Configure Fetch</h4>
                    <p class="text-slate-400 mb-6">Configure remote tracking to enable standard fetching in a bare environment.</p>
                    <CodeBlock code='git config remote.origin.fetch "+refs/heads/*:refs/remotes/origin/*"' label="Terminal" />
                </div>

                <!-- Step 4 -->
                <div class="fade-in relative bg-slate-800 p-8 rounded-xl border border-slate-700 shadow-sm">
                    <div class="absolute -left-4 top-8 w-8 h-8 bg-blue-600 rounded-full flex items-center justify-center font-bold text-white text-sm hidden md:flex">4</div>
                    <h4 class="text-xl font-bold text-white mb-3">Create Worktrees</h4>
                    <p class="text-slate-400 mb-6">Generate dedicated folders for your active branches.</p>
                    <CodeBlock code={`git worktree add main
git worktree add -b feature-scaling scaling`} label="Terminal" />
                </div>

            </div>
        </div>
    </div>
</section>
