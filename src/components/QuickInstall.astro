---
import CodeBlock from './CodeBlock.astro';
---

<section class="py-12 bg-slate-900 border-y border-slate-800">
    <div class="container mx-auto px-4">
        <div class="flex flex-col lg:flex-row items-center justify-between gap-12">
            <div class="max-w-xl text-center lg:text-left">
                <h2 class="text-3xl font-bold text-white mb-4 tracking-tight">Set up the CLI</h2>
                <p class="text-slate-400 leading-relaxed font-mono text-sm border-l-2 border-blue-500 pl-4">
                   Install the <code>worktrees</code> engine to automate your Bare Hub workflow. 
                   Optimized for performance and rapid execution.
                </p>
            </div>
            
            <div class="w-full lg:w-auto flex flex-col sm:flex-row gap-6">
                <div class="space-y-3">
                    <span class="text-xs font-bold text-slate-500 uppercase tracking-widest pl-1">macOS / Linux</span>
                    <CodeBlock code={`curl --proto '=https' --tlsv1.2 -LsSf https://github.com/justinnathanielsmith/worktrees/releases/latest/download/worktrees-installer.sh | sh`} label="Shell" />
                </div>
                <div class="space-y-3">
                    <span class="text-xs font-bold text-slate-500 uppercase tracking-widest pl-1">Windows</span>
                    <CodeBlock code={`powershell -ExecutionPolicy ByPass -c "irm https://github.com/justinnathanielsmith/worktrees/releases/latest/download/worktrees-installer.ps1 | iex"`} label="PowerShell" />
                </div>
            </div>
        </div>
    </div>
</section>
