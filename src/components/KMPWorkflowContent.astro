---
import CodeBlock from './CodeBlock.astro';
---

<section class="py-24 space-y-32 container mx-auto px-4">
    <!-- Header -->
    <div class="max-w-4xl reveal">
        <h1 class="text-6xl md:text-8xl font-black text-white impact mb-8 leading-none">
            KMP <br>
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-cyber-cyan to-cyber-pink">WORKFLOW</span>
        </h1>
        <p class="text-xl text-slate-400 font-mono tracking-tight leading-relaxed border-l-4 border-cyber-cyan pl-6">
            Mastering Kotlin Multiplatform (KMP) in a Bare Hub environment. 
            Optimize Gradle performance, manage shared caches, and orchestrate parallel builds across Android, iOS, and Web.
        </p>
    </div>

    <!-- 01. The Cache Strategy -->
    <div class="reveal space-y-12">
        <div class="flex items-center gap-4">
            <span class="text-cyber-cyan font-black text-4xl impact">01</span>
            <h2 class="text-4xl font-black text-white impact tracking-widest uppercase">The Cache Strategy</h2>
        </div>
        
        <div class="space-y-8">
            <p class="text-slate-300 leading-relaxed">
                KMP builds are notoriously heavy. Without a global cache, every worktree will redundantly build the same modules, leading to massive disk bloat and wasted CPU cycles.
            </p>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                <div class="bg-slate-900/50 p-8 border border-slate-800 rounded-xl">
                    <h3 class="text-xl font-bold text-cyber-cyan impact mb-4 uppercase">Enable Build Cache</h3>
                    <p class="text-sm text-slate-400 mb-6">Forces Gradle to store task outputs in a global location accessible by all worktrees.</p>
                    <CodeBlock code={`# gradle.properties
org.gradle.caching=true`} label="GRADLE_CONFIG" />
                </div>
                <div class="bg-slate-900/50 p-8 border border-slate-800 rounded-xl">
                    <h3 class="text-xl font-bold text-cyber-pink impact mb-4 uppercase">Explicit Global Path</h3>
                    <p class="text-sm text-slate-400 mb-6">Point all worktrees to a specific high-speed disk or shared mount.</p>
                    <CodeBlock code={`// settings.gradle.kts
buildCache {
    local {
        directory = File("\${System.getProperty("user.home")}/.gradle-global-cache")
        removeUnusedEntriesAfterDays = 30
    }
}`} label="SETTINGS_GRADLE" />
                </div>
            </div>
        </div>
    </div>

    <!-- 02. KMP Specific Optimization -->
    <div class="reveal space-y-12">
        <div class="flex items-center gap-4">
            <span class="text-cyber-pink font-black text-4xl impact">02</span>
            <h2 class="text-4xl font-black text-white impact tracking-widest uppercase">KMP Optimization</h2>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
            <div class="space-y-6">
                <h3 class="text-2xl font-black text-white impact uppercase">Share the .konan Directory</h3>
                <p class="text-slate-400 text-sm leading-relaxed">
                    Kotlin/Native downloads massive compilers and sysroots to `~/.gradle/konan`. Ensure all worktrees point to the same directory to avoid gigabytes of redundant downloads.
                </p>
                <div class="p-6 bg-slate-800/40 rounded-xl border border-slate-700">
                    <p class="text-[0.65rem] font-mono text-cyber-cyan uppercase mb-2">Pro-Tip</p>
                    <p class="text-xs text-slate-500">Modern versions of Kotlin (1.9.0+) have relocatable tasks. Ensure your custom tasks don't use absolute paths to avoid cache misses.</p>
                </div>
            </div>
            <div class="space-y-6">
                <h3 class="text-2xl font-black text-white impact uppercase">Global node_modules</h3>
                <p class="text-slate-400 text-sm leading-relaxed">
                    For Compose HTML or JS targets, force Gradle to use a central Yarn/NPM cache to prevent `node_modules` explosion.
                </p>
                <CodeBlock code={`rootProject.plugins.withType<org.jetbrains.kotlin.gradle.targets.js.yarn.YarnPlugin> {
    rootProject.the<org.jetbrains.kotlin.gradle.targets.js.yarn.YarnRootExtension>().download = true
}`} label="JS_YARN_CONFIG" />
            </div>
        </div>
    </div>

    <!-- 03. Identity & Secrets -->
    <div class="reveal space-y-12">
        <div class="flex items-center gap-4">
            <span class="text-cyber-green font-black text-4xl impact">03</span>
            <h2 class="text-4xl font-black text-white impact tracking-widest uppercase">Identity & Secrets</h2>
        </div>

        <div class="bg-slate-900/50 p-10 rounded-3xl border border-slate-800 space-y-8">
            <div class="flex flex-col md:flex-row gap-8 items-start">
                <div class="md:w-1/2 space-y-4">
                    <h3 class="text-2xl font-black text-white impact">THE SYMLINK TRICK</h3>
                    <p class="text-slate-400 text-sm leading-relaxed">
                        Each worktree needs its own `local.properties` and service files (Firebase, API keys). Instead of copying, use symlinks to keep everything synced with the central hub.
                    </p>
                </div>
                <div class="md:w-1/2 w-full">
                    <CodeBlock code={`# Symlink from hub to worktree
ln -s /path/to/hub/local.properties /path/to/worktree/local.properties`} label="SHELL // SYMLINK" />
                </div>
            </div>
        </div>
    </div>

    <!-- 04. Automation: The KMP-Worktree Script -->
    <div class="reveal space-y-12">
        <div class="flex items-center gap-4">
            <span class="text-white font-black text-4xl impact">04</span>
            <h2 class="text-4xl font-black text-white impact tracking-widest uppercase">Automation</h2>
        </div>

        <div class="space-y-6">
            <p class="text-slate-300">Automate the setup of new KMP environments with this robust architect script.</p>
            <CodeBlock code={`function kwt() {
    local BRANCH_NAME=$1
    local TARGET_DIR="../$BRANCH_NAME"

    # 1. Create Worktree
    git worktree add -b "$BRANCH_NAME" "$TARGET_DIR"

    # 2. Symlink Configs
    [ -f "local.properties" ] && ln -sf "$(pwd)/local.properties" "$TARGET_DIR/local.properties"
    [ -f "composeApp/google-services.json" ] && ln -sf "$(pwd)/composeApp/google-services.json" "$TARGET_DIR/composeApp/google-services.json"

    # 3. Silent Warmup
    echo "🐘 Warming up Gradle..."
    (cd "$TARGET_DIR" && ./gradlew help > /dev/null 2>&1 &)

    cd "$TARGET_DIR"
}`} label="BASH // KMP_ARCHITECT" />
        </div>
    </div>

    <!-- 05. Resource Table -->
    <div class="reveal space-y-12">
        <div class="flex items-center gap-4">
            <span class="text-yellow-500 font-black text-4xl impact">05</span>
            <h2 class="text-4xl font-black text-white impact tracking-widest uppercase">Summary</h2>
        </div>

        <div class="bg-slate-900/50 border border-slate-800 rounded-2xl overflow-hidden">
            <table class="w-full text-left text-sm">
                <thead class="bg-slate-800/50 text-cyber-cyan impact tracking-widest uppercase text-xs">
                    <tr>
                        <th class="px-6 py-4">Scenario</th>
                        <th class="px-6 py-4">Recommendation</th>
                        <th class="px-6 py-4">Setup Effort</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-800 text-slate-400 font-mono">
                    <tr>
                        <td class="px-6 py-4 text-white font-bold">Local Worktrees</td>
                        <td class="px-6 py-4 text-cyber-pink">org.gradle.caching=true</td>
                        <td class="px-6 py-4 text-cyber-green">LOW (1 LINE)</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 text-white font-bold">Multi-Agent (Same Machine)</td>
                        <td class="px-6 py-4 text-cyber-pink">Shared local directory</td>
                        <td class="px-6 py-4 text-cyber-pink">MEDIUM</td>
                    </tr>
                    <tr>
                        <td class="px-6 py-4 text-white font-bold">Multi-Agent (Distributed)</td>
                        <td class="px-6 py-4 text-cyber-pink">Remote HTTP Cache</td>
                        <td class="px-6 py-4 text-red-500">HIGH</td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</section>
